<% if flash.any? %>
  <div class="flashes">
    <% user_facing_flashes.each do |key, value| -%>
      <div class="alert alert-info flash-<%= key %>"><%= value %></div>
    <% end -%>
  </div>
<% end %>
